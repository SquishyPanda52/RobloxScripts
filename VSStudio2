local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")

local player = Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local hrp = char:WaitForChild("HumanoidRootPart")
local cam = workspace.CurrentCamera
local playerGui = player:WaitForChild("PlayerGui")

print("Fly Script Loaded - Memory Only Execution")

local gui = Instance.new("ScreenGui")
gui.Name = "FlyUI"
gui.ResetOnSpawn = false
gui.Parent = playerGui

-- container frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 240, 0, 140)
frame.Position = UDim2.new(0.5, -120, 0.5, -70)
frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
frame.BorderSizePixel = 0
frame.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 8)
corner.Parent = frame

local grad = Instance.new("UIGradient")
grad.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(128,0,128)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255,0,255))
}
grad.Parent = frame

-- title bar for drag
local titlebar = Instance.new("Frame")
titlebar.Size = UDim2.new(1,0,0,24)
titlebar.BackgroundTransparency = 1
titlebar.Parent = frame

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -60, 1, 0)
title.Position = UDim2.new(0,8,0,0)
title.BackgroundTransparency = 1
title.Text = "Fly UI"
title.TextColor3 = Color3.new(1,1,1)
title.Font = Enum.Font.GothamBold
title.TextSize = 14
title.Parent = titlebar

local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0,20,0,20)
closeBtn.Position = UDim2.new(1, -24, 0, 2)
closeBtn.Text = "X"
closeBtn.Font = Enum.Font.Gotham
closeBtn.TextSize = 14
closeBtn.BackgroundTransparency = 0.5
closeBtn.BackgroundColor3 = Color3.fromRGB(200,50,50)
closeBtn.TextColor3 = Color3.new(1,1,1)
closeBtn.Parent = titlebar

local minBtn = Instance.new("TextButton")
minBtn.Size = UDim2.new(0,20,0,20)
minBtn.Position = UDim2.new(1, -48, 0, 2)
minBtn.Text = "-"
minBtn.Font = Enum.Font.Gotham
minBtn.TextSize = 14
minBtn.BackgroundTransparency = 0.5
minBtn.BackgroundColor3 = Color3.fromRGB(100,100,100)
minBtn.TextColor3 = Color3.new(1,1,1)
minBtn.Parent = titlebar

local btn = Instance.new("TextButton")
btn.Name = "FlyBtn"
btn.Size = UDim2.new(0, 100, 0, 30)
btn.Position = UDim2.new(0.5, -50, 0.6, -15)
btn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
btn.TextColor3 = Color3.new(1, 1, 1)
btn.Font = Enum.Font.GothamBold
btn.TextSize = 16
btn.Text = "Fly ON"
btn.Parent = frame

local spd = Instance.new("TextLabel")
spd.Name = "SpeedLabel"
spd.Size = UDim2.new(0, 100, 0, 20)
spd.Position = UDim2.new(0.5, -50, 0.82, -20)
spd.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
spd.TextColor3 = Color3.new(1, 1, 1)
spd.Font = Enum.Font.Gotham
spd.TextSize = 14
spd.Text = "Speed: 50"
spd.Parent = gui

local flying = false
local flySpeed = 50
local maxSpeed = 150
local accel = 0.2
local vel = Vector3.new(0, 0, 0)
local bg, bv

local function startFly()
	bg = Instance.new("BodyGyro")
	bg.P = 9000
	bg.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
	bg.CFrame = hrp.CFrame
	bg.Parent = hrp
	
	bv = Instance.new("BodyVelocity")
	bv.MaxForce = Vector3.new(9e9, 9e9, 9e9)
	bv.Velocity = Vector3.new(0, 0, 0)
	bv.Parent = hrp
	
	flying = true
	btn.Text = "Fly OFF"
	btn.BackgroundColor3 = Color3.fromRGB(255, 85, 85)
end

local function stopFly()
	flying = false
	if bg then bg:Destroy() end
	if bv then bv:Destroy() end
	vel = Vector3.new(0, 0, 0)
	btn.Text = "Fly ON"
	btn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
end

btn.MouseButton1Click:Connect(function()
	if flying then
		stopFly()
	else
		startFly()
	end
end)

RunService.RenderStepped:Connect(function()
	if not flying or not bg or not bv then return end
	
	local moveVel = Vector3.new(0, 0, 0)
	local currentSpeed = flySpeed
	
	if UIS:IsKeyDown(Enum.KeyCode.LeftShift) then
		currentSpeed = math.min(flySpeed + 30, maxSpeed)
	end
	
	if UIS:IsKeyDown(Enum.KeyCode.W) then
		moveVel = moveVel + cam.CFrame.LookVector * currentSpeed
	end
	if UIS:IsKeyDown(Enum.KeyCode.S) then
		moveVel = moveVel - cam.CFrame.LookVector * currentSpeed
	end
	if UIS:IsKeyDown(Enum.KeyCode.D) then
		moveVel = moveVel + cam.CFrame.RightVector * currentSpeed
	end
	if UIS:IsKeyDown(Enum.KeyCode.A) then
		moveVel = moveVel - cam.CFrame.RightVector * currentSpeed
	end
	if UIS:IsKeyDown(Enum.KeyCode.Space) then
		moveVel = moveVel + Vector3.new(0, currentSpeed, 0)
	end
	if UIS:IsKeyDown(Enum.KeyCode.LeftControl) then
		moveVel = moveVel - Vector3.new(0, currentSpeed, 0)
	end
	
	vel = vel:Lerp(moveVel, accel)
	bv.Velocity = vel
	bg.CFrame = cam.CFrame
	spd.Text = "Speed: " .. math.floor(currentSpeed)
end)

UIS.InputBegan:Connect(function(input, gp)
	if gp then return end
	if input.KeyCode == Enum.KeyCode.F then
		if flying then stopFly() else startFly() end
	end
end)
